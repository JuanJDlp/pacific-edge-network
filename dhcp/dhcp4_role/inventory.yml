---
# ============================================================
#  inventory.yml
#  Inventario — Red Comunitaria Cerrito Bongo & Cocalito
#  Administración de Plataformas I — Universidad ICESI
# ============================================================

all:
  vars:
    ansible_python_interpreter: /usr/bin/python3
    ansible_become: true
    ansible_ssh_private_key_file: '~/.ssh/plats_mini_pc'
    ansible_ssh_common_args: '-o StrictHostKeyChecking=no'


  children:

    # Mini PC — núcleo de red: router, firewall y servidor DHCP/DNS
    minipc:
      hosts:
        minipc-core:
          ansible_host: 100.90.95.134
          ansible_user: user

