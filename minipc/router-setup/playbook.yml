---
# ============================================================
# Playbook principal — Mini PC Pacific Edge Network
# Automatiza todos los servicios del router/gateway
#
# Uso completo:
#   ansible-playbook -i inventory.ini playbook.yml
#
# Solo un rol:
#   ansible-playbook -i inventory.ini playbook.yml --tags nftables
#   ansible-playbook -i inventory.ini playbook.yml --tags dns
#   ansible-playbook -i inventory.ini playbook.yml --tags dhcp
#   ansible-playbook -i inventory.ini playbook.yml --tags captive_portal
#   ansible-playbook -i inventory.ini playbook.yml --tags ntp
#   ansible-playbook -i inventory.ini playbook.yml --tags monitoring
#   ansible-playbook -i inventory.ini playbook.yml --tags firewall
#   ansible-playbook -i inventory.ini playbook.yml --tags pihole
#
# Dry-run:
#   ansible-playbook -i inventory.ini playbook.yml --check
#
# Verificación:
#   ansible-playbook -i inventory.ini playbook.yml --tags verify
# ============================================================

- name: Configurar Mini PC — router y servicios
  hosts: minipc
  become: true
  gather_facts: true

  pre_tasks:
    - name: Actualizar cache de apt
      apt:
        update_cache: true
        cache_valid_time: 3600
      tags: always

  roles:
    # 1. Red: interfaces, VLANs, IP forwarding, nftables, Jool NAT64
    - role: router
      tags: [router, nftables, netplan]

    # 2. DNS — Bind9 con dominio biblioteca.local (reemplaza systemd-resolved en VLANs)
    - role: dns
      tags: [dns]

    # 3. DHCP — Kea DHCPv4 con reserva para RPi y DNS apuntando a Bind9
    - role: dhcp
      tags: [dhcp]

    # 4. Portal cautivo + nginx HTTP proxy intermediario hacia Squid en RPi
    #    Resuelve el problema SO_ORIGINAL_DST del DNAT cross-machine
    - role: captive_portal
      tags: [captive_portal, portal]

    # 5. NTP — chrony como servidor para todas las VLANs internas
    - role: ntp
      tags: [ntp]

    # 6. Monitoreo — Prometheus + Grafana + node_exporter
    - role: monitoring
      tags: [monitoring]

    # 7. Firewall mejorado — reemplaza nftables del rol router con reglas
    #    más robustas: rate limiting SSH/DNS/ICMP, bloqueo de puertos WAN,
    #    aislamiento estricto entre VLANs, logging de drops
    - role: firewall
      tags: [firewall, nftables]

    # 8. Pi-hole — DNS con bloqueo de publicidad y contenido no deseado
    #    Corre como contenedor Docker, escucha en 192.168.10.1:53
    #    El DNAT de nftables fuerza todo el DNS de las VLANs hacia Pi-hole
    - role: pihole
      tags: [pihole, dns]

# ─── Play 2: Verificación ────────────────────────────────────────────────────

- name: Verificar configuración del Mini PC
  hosts: minipc
  become: true
  gather_facts: false
  tags: [verify, never]

  tasks:

    # ── Red básica ────────────────────────────────────────────────────────────
    - name: Verificar ip_forward IPv4
      command: sysctl net.ipv4.ip_forward
      register: ipv4_fwd
      changed_when: false

    - name: Assert ip_forward = 1
      assert:
        that: "'= 1' in ipv4_fwd.stdout"
        fail_msg: "ip_forward IPv4 no está habilitado"

    - name: Verificar rutas VLAN en tabla de routing
      command: ip route show
      register: route_table
      changed_when: false

    - name: Assert rutas VLAN presentes
      assert:
        that:
          - "'192.168.10.0/24' in route_table.stdout"
          - "'192.168.20.0/24' in route_table.stdout"
          - "'192.168.30.0/24' in route_table.stdout"
        fail_msg: "Faltan rutas VLAN en la tabla de routing"

    # ── nftables ──────────────────────────────────────────────────────────────
    - name: Verificar set captive_allowed
      command: nft list set inet filter captive_allowed
      changed_when: false

    - name: Verificar DNAT portal en nat prerouting
      command: nft list chain ip nat prerouting
      register: nft_nat
      changed_when: false

    - name: Assert DNAT portal cautivo presente
      assert:
        that: "'2050' in nft_nat.stdout"
        fail_msg: "DNAT del portal cautivo (puerto 2050) no encontrado en nftables"

    # ── DNS ───────────────────────────────────────────────────────────────────
    - name: Verificar Bind9 responde en 192.168.10.1
      command: dig @192.168.10.1 biblioteca.local +short
      register: dns_check
      changed_when: false
      failed_when: dns_check.rc != 0 or dns_check.stdout == ""

    - name: Mostrar IP resuelta para biblioteca.local
      debug:
        msg: "biblioteca.local → {{ dns_check.stdout }}"

    # ── Servicios ─────────────────────────────────────────────────────────────
    - name: Verificar Kea DHCP4 activo
      command: systemctl is-active kea-dhcp4-server
      changed_when: false
      failed_when: false
      register: kea_active

    - name: Assert Kea activo
      assert:
        that: kea_active.stdout == 'active'
        fail_msg: "Kea DHCP4 no está activo"

    - name: Verificar nginx portal (puerto 2050)
      wait_for:
        port: 2050
        timeout: 5

    - name: Verificar nginx http-proxy (puerto 8888)
      wait_for:
        port: 8888
        timeout: 5

    - name: Verificar captive-accept (puerto 2051)
      wait_for:
        host: 127.0.0.1
        port: 2051
        timeout: 5

    - name: Verificar ping a internet (8.8.8.8)
      command: ping -c 2 -W 2 8.8.8.8
      changed_when: false
      failed_when: false
      register: ping_inet

    # ── Pi-hole ───────────────────────────────────────────────────────────────
    - name: Verificar Pi-hole responde en puerto 53
      wait_for:
        host: 192.168.10.1
        port: 53
        timeout: 10
      ignore_errors: true
      register: pihole_port_check

    - name: Verificar panel web Pi-hole (puerto 8080)
      wait_for:
        host: 192.168.10.1
        port: 8080
        timeout: 10
      ignore_errors: true
      register: pihole_web_check

    # ── Firewall ──────────────────────────────────────────────────────────────
    - name: Verificar set ssh_bruteforce existe en nftables
      command: nft list set inet filter ssh_bruteforce
      changed_when: false
      ignore_errors: true
      register: nft_ssh_set

    - name: Verificar rate limiting SSH en nftables
      command: nft list chain inet filter input
      register: nft_input_chain
      changed_when: false

    - name: Assert rate limiting SSH presente
      assert:
        that: "'ssh_bruteforce' in nft_input_chain.stdout"
        fail_msg: "Rate limiting SSH no encontrado en nftables"

    - name: Resumen final
      debug:
        msg:
          - "ip_forward:        {{ ipv4_fwd.stdout }}"
          - "biblioteca.local:  {{ dns_check.stdout }}"
          - "ping internet:     {{ 'OK' if ping_inet.rc == 0 else 'FAIL (sin WAN)' }}"
          - "Pi-hole DNS :53:   {{ 'OK' if pihole_port_check is not failed else 'FAIL' }}"
          - "Pi-hole web :8080: {{ 'OK' if pihole_web_check is not failed else 'FAIL' }}"
          - "SSH bruteforce set:{{ 'OK' if nft_ssh_set is not failed else 'FAIL' }}"
          - "Todos los servicios verificados correctamente"
