---
# Playbook de servicio — stack de monitoreo completo
# cd minipc/ && ansible-playbook -i router-setup/inventory.ini services/monitoring.yml

- name: Importar playbook de monitoreo
  ansible.builtin.import_playbook: ../router-setup/playbook-monitoring.yml
